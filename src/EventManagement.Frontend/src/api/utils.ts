import { AxiosStatic } from 'axios';
import axios from './axiosProvider';
import { useMutation as queryUseMutation } from '@tanstack/react-query';

// export const useAxios = () => {
//   return {
//     mutation: useAxiosMutat
//   }
// };

// const useAxiosMutation = () => {
//   const useMutationFunction: useAxiosMutationType =
//   return ();
// }

// type useAxiosMutationType = (axios: AxiosStatic, useMutation: typeof queryUseMutation ) => ReturnType<typeof queryUseMutation>;